version: '3.8'

services:
  blog:
    build: .
    container_name: simple-blog
    ports:
      - "80:80"
    # 可选：设置重启策略
    restart: unless-stopped
    # 可选：设置环境变量
    environment:
      - NODE_ENV=production
    # 可选：挂载卷（用于持久化日志等）
    volumes:
      - ./logs:/var/log/nginx
